# EduBridge — Environment Variables Template
# Copy to .env:  cp .env.example .env

# Generate your own:  python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-your-own-generated-key

# Free Gemini AI key — get it at https://aistudio.google.com/apikey
# No credit card. No billing. Just a Google account.
GEMINI_API_KEY=paste-your-AIza-key-here

FLASK_ENV=development
PORT=5000
DATA_DIR=data
